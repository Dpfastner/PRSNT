# @prsnt/mobile

React Native + Expo app — iOS and Android.

## Run

```bash
pnpm dev          # Expo dev server
pnpm ios          # iOS simulator
pnpm android      # Android emulator
```

## Scope (v1)

Profiles + Secret Santa + Birthdays. See `docs/STRATEGY.md` §6 for the build order.
