# Expo + React Native do internal `require('expo-modules-core')`-style lookups
# that assume a hoisted node_modules layout. pnpm's default isolated layout
# breaks those. Per Expo's official monorepo guide, use a hoisted linker.
node-linker=hoisted
